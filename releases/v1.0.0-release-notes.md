# Aireader v1.0.0 Release Notes

**Release Date**: 2026-02-07  
**Platform**: Windows x64

---

## 🎉 First Stable Release / 首个正式版

Aireader is an AI-powered desktop reading assistant that runs **100% locally**. Your documents never leave your machine.

Aireader 是一款本地 AI 驱动的桌面阅读助手，AI 推理完全在本地运行，你的文档永远不会离开你的电脑。

---

## ✨ Features / 功能

### 📖 Multi-Format Reader / 多格式阅读
- PDF continuous scroll with zoom, page navigation, internal links
- EPUB paginated & scroll modes with TOC tracking
- Markdown rendering (headings, code blocks, tables, math, images)
- TXT plain text with word wrap
- Auto-saved reading progress per document

### 🤖 Local AI Inference / 本地 AI 推理
- **Built-in llama.cpp** — zero-config, works offline out of the box
- Auto hardware detection: CUDA / Vulkan / CPU
- **Smart 3-layer tiering**: hardware detection → resource pre-filter → llama-bench benchmark (tok/s) → precise model selection
- One-click setup: detect hardware → benchmark → recommend model → download → start
- Built-in Qwen3 models (0.6B ~ 8B, multiple tiers)
- **Model downgrade button** — one-click switch to a smaller model if too slow
- Integrated GPU (Intel UHD/HD/Iris, VRAM < 2GB) auto-fallback to CPU mode
- Also supports **Ollama** and **OpenAI-compatible APIs**

### 🔤 Select to Translate / 选中即译
- Three modes: Literal / Free / Plain language
- Auto-detects Chinese↔English direction
- Complex sentence auto-breakdown

### 📝 Grammar Explain / 文法解释
- Breaks down sentence structure and vocabulary usage

### 💬 Contextual Chat / 上下文对话
- ChatGPT-style chat about selected text
- Context locking — select text, then ask follow-up questions
- Persistent chat history per document
- 🧠 Deep Thinking toggle — Qwen3 true thinking mode (truly disables when off)

### 📒 Smart Notes / 智能笔记
- Save translations & explanations as notes with one click
- Three types: AI Generated → Confirmed → User Note
- Notes linked to documents, auto-loaded on switch
- Export as Markdown

### 📚 Offline Dictionary / 离线词典
- Built-in ECDICT (English→Chinese) + CC-CEDICT (Chinese→English)
- Double-click any word to look up
- Bidirectional Chinese-English lookup, each toggleable in Settings

### 📁 Document Library / 文档库
- Import Copy (recommended) or Open Directly
- Search, sort, filter documents
- Custom document library directory
- Batch folder import

### 🔧 Configuration / 配置
- First-launch setup wizard (language → storage paths → AI one-click setup)
- Separate configurable model storage directory (independent from runtime)
- Model directory migration support when changing paths
- Connection test for Ollama and OpenAI-compatible API
- Custom model download URLs
- All settings auto-save immediately

### 🎨 UI / 界面
- Dark / Light theme with system detection
- Independent document area theme toggle
- Bilingual UI (Chinese & English)
- Resizable panel layout
- Floating reader toolbar

---

## 🖥️ System Requirements / 系统要求

| | Minimum | Recommended |
|:---|:---|:---|
| **OS** | Windows 10 x64 | Windows 11 x64 |
| **RAM** | 4 GB | 8 GB+ |
| **Disk** | 500 MB (app + CPU runtime) | 2 GB+ (with models) |
| **GPU** | Not required (CPU mode) | NVIDIA (CUDA) or AMD/Intel (Vulkan) |

---

## 📦 Installation / 安装

1. Download `Aireader_1.0.0_x64-setup.exe`
2. Run the installer (supports per-user or system-wide install)
3. Launch Aireader → First-launch setup wizard guides you through language, storage paths, and AI configuration
4. Or skip the wizard and configure later in Settings → AI → **One-Click Setup**

GPU users will auto-download the appropriate runtime (CUDA/Vulkan) on first use.

> **Note**: Integrated GPUs (Intel UHD/HD/Iris) with < 2GB VRAM will automatically use CPU mode for better performance. The system uses llama-bench to measure actual inference speed and recommend the optimal model.
>
> **注意**: 集成显卡（Intel UHD/HD/Iris，显存 < 2GB）会自动选择 CPU 模式以获得更好性能。系统会通过 llama-bench 实测推理速度，自动推荐最佳模型。

---

## 🔄 Update / 更新

Download and run the new installer. It will overwrite the previous version automatically. Your documents, notes, models and settings are preserved.

下载新版安装包直接运行即可，自动覆盖旧版本。文档、笔记、模型和设置均会保留。

---

## 🗑️ Uninstall / 卸载

**Method 1**: Windows Settings → Apps → search "Aireader" → Uninstall

**Method 2**: Run `Uninstall Aireader.exe` in the installation directory

**User data** (documents, notes, models) is stored in `%APPDATA%/com.aireader.app/` and is **not** removed by uninstall. Delete it manually if needed.

**方法一**: Windows 设置 → 应用 → 搜索 "Aireader" → 卸载

**方法二**: 运行安装目录下的 `Uninstall Aireader.exe`

**用户数据**（文档、笔记、模型）存储在 `%APPDATA%/com.aireader.app/`，卸载时**不会**删除。如需彻底清理请手动删除该目录。

---

## ⚠️ Known Limitations / 已知限制

- Windows x64 only (macOS/Linux planned)
- Full-document chat not yet supported (select text first)
- Scanned PDF (image-based) text selection not supported
- Built-in models are Qwen3 series only

---

## 🙏 Acknowledgments / 致谢

- [llama.cpp](https://github.com/ggerganov/llama.cpp) — Local LLM inference engine
- [Tauri](https://tauri.app) — Desktop application framework
- [Qwen3](https://github.com/QwenLM/Qwen3) — Built-in language models
- [ECDICT](https://github.com/skywind3000/ECDICT) — Offline English-Chinese dictionary

---

## License

[MIT](LICENSE) © xujiayu
