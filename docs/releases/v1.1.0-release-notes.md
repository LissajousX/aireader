# Aireader v1.1.0 Release Notes

**Release Date**: 2026-02-13  
**Platforms**: Windows x64 · macOS ARM / Intel · Linux x64 (including Ubuntu 20.04+)

---

## 🎯 版本亮点 / Highlights

v1.1.0 是自 v1.0.6 以来的重要更新，**核心目标是全面支持 Ubuntu 20.04 (Focal Fossa)**，同时修复了多个关键 bug 并优化了用户引导体验。

v1.1.0 is a significant update since v1.0.6, with the **primary goal of full Ubuntu 20.04 (Focal Fossa) support**, along with critical bug fixes and improved user guidance.

---

## 🐧 Ubuntu 20.04 全面支持 / Full Ubuntu 20.04 Support

### 构建基础设施 / Build Infrastructure
- 基于 Docker 容器的 focal 专用构建环境，包含自编译的 WebKitGTK 2.42.5 (GCC 11)
- 独立的 `build-focal.yml` CI 工作流，自动化 AppImage 构建与发布
- AppImage 内注入 GCC 11 的 `libstdc++.so.6`，确保在 focal 系统库下正常运行

### llama.cpp 运行时 / llama.cpp Runtime
- CI 使用 GCC 11 在 focal 容器内**自编译 llama.cpp b7966**（CPU + Vulkan 双版本）
- 编译时设置 `CMAKE_BUILD_RPATH=$ORIGIN`，确保 binary 在任意目录下都能找到同目录的 `.so` 文件
- 打包时使用 `cp -a lib*.so*` 正确保留版本号符号链接（如 `libllama.so.0`）
- Rust 后端为 `llama-server`、`llama-bench`、`validate_runtime_binary` 设置 `LD_LIBRARY_PATH` 兜底
- glibc < 2.34 的系统自动使用捆绑运行时，跳过在线下载（避免 GLIBC 不兼容）

### PDF 兼容性 / PDF Compatibility
- 为 WebKitGTK 2.42 的 JavaScriptCore 引擎添加 `Promise.withResolvers` (ES2024) polyfill
- 在老引擎上预加载 PDF worker 到主线程（`globalThis.pdfjsWorker`），避免 Web Worker 解析时崩溃导致 PDF 永久卡在 "Parsing PDF..."
- 始终设置 `workerSrc` 为有效 URL，防止裸模块名 `pdf.worker.mjs` 在 WebKitGTK 中报错

---

## 🐛 Bug 修复 / Bug Fixes

- **[Linux] libllama.so.0 找不到**: CI 打包 `.so` 文件时丢失版本号符号链接 + binary RPATH 指向构建机路径。通过 `CMAKE_BUILD_RPATH=$ORIGIN` + `cp -a lib*.so*` + `LD_LIBRARY_PATH` 三层修复
- **[Linux] PDF 永久卡在 "Parsing PDF..."**: pdfjs Web Worker 通过初始握手但在解析时调用 `Promise.withResolvers` 崩溃，pdfjs 未触发回退。通过主线程预加载 worker 模块彻底绕过真实 Worker
- **[Linux] GLIBC_2.34 不兼容**: 在 glibc < 2.34 系统上，自动检测并使用捆绑的自编译运行时，移除不兼容的已下载运行时
- **[Windows] CUDA DLL 加载路径修正**: 改进 Windows 上的 `PATH` 环境变量设置
- **[All] 模型下载误阻断**: 修复 bundled-only 模式错误地阻止模型下载的回归 bug，增加回归测试覆盖

---

## ✨ 新功能与优化 / New Features & Improvements

### 🎓 用户引导优化
- **设置向导**和**设置页**新增提示：内置 AI 为可选配置，用户可跳过直接使用 Ollama 或 OpenAI 兼容 API
- 提示以淡色信息条呈现，简约不干扰

### 🏗️ CI/CD 改进
- 重构为独立的按平台 CI 工作流 + 编排器模式
- 新增 focal Docker 构建镜像（`ghcr.io/lissajousx/aireader-focal-build`）
- macOS 签名与公证流程优化
- 321 个自动化测试覆盖核心模块（Rust 58 + 前端 263）

---

## 🖥️ 平台支持 / Platform Support

| 平台 | 架构 | 安装包 | GPU 加速 | 最低系统 |
|------|------|--------|---------|---------|
| **Windows** | x64 | `.exe` (NSIS) | CUDA / Vulkan | Windows 10 |
| **macOS** | Apple Silicon | `.dmg` | Metal (内置) | macOS 11+ |
| **macOS** | Intel | `.dmg` | Metal (内置) | macOS 11+ |
| **Linux** | x64 | `.AppImage` / `.deb` | Vulkan | Ubuntu 22.04+ |
| **Linux (Focal)** | x64 | `.AppImage` | Vulkan | **Ubuntu 20.04** |

---

## 📦 下载 / Downloads

| 文件 | 平台 |
|------|------|
| `Aireader_1.1.0_x64-setup.exe` | Windows x64 |
| `Aireader_1.1.0_aarch64.dmg` | macOS Apple Silicon |
| `Aireader_1.1.0_x64.dmg` | macOS Intel |
| `Aireader_1.1.0_amd64.AppImage` | Linux x64 (Ubuntu 22.04+) |
| `Aireader_1.1.0_amd64.deb` | Linux x64 (Debian/Ubuntu 22.04+) |
| `Aireader_1.1.0_amd64-focal.AppImage` | Linux x64 (**Ubuntu 20.04**) |

---

## 🔄 升级说明 / Upgrade

从 v1.0.x 升级：直接下载新版安装即可，文档、笔记、模型和设置均会保留。

Upgrade from v1.0.x: simply download and install the new version. Documents, notes, models and settings are preserved.

---

## License

[MIT](../../LICENSE) © xujiayu
